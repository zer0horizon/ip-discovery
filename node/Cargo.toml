[package]
name = "ip-discovery-node"
version = "0.4.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16", features = ["async", "tokio_rt"] }
napi-derive = "2.16"
ip-discovery = { path = "..", features = ["all"] }
